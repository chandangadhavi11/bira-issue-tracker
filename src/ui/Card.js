import styled from "styled-components";
import { ContentBox } from "./ContentBox";

export const Card = styled(ContentBox)`
    min-width: 300px;
    background: #FFFFFF;
    box-shadow: 0px 4px 20px rgba(0, 0, 0, 0.25);
    border-radius: 4px;
    padding: 24px;
`